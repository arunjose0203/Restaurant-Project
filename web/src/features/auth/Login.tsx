import {ArrowRight,Eye,EyeOff,ShieldCheck,UtensilsCrossed} from 'lucide-react';
import {useState} from 'react';
import {login} from '../../api';
import type {User} from '../../types';
export function Login({onLogin}:{onLogin:(u:User)=>void}){
 const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [visible,setVisible]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);
 return <div className="login-page"><aside className="login-story"><div className="brand"><span className="brand-icon"><UtensilsCrossed size={22}/></span>tableflow.</div><div className="login-art" aria-hidden="true"/><h2>Good food.<br/>Great company.<br/>Smoother service.</h2><p>From the first order to the last goodbye. A little more room for hospitality.</p><small>YOUR RESTAURANT, WORKING TOGETHER</small></aside><div className="login-card"><div className="eyebrow">WELCOME TO YOUR SHIFT</div><h1>Let’s make it a good one.</h1><p>Sign in to your restaurant workspace.</p><form onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');try{onLogin(await login(email.trim(),password));}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><label className="field-label">Email address<input autoComplete="username" type="email" required placeholder="you@restaurant.com" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field-label">Password<div className="password-field"><input autoComplete="current-password" type={visible?'text':'password'} required placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="icon-button" type="button" aria-label={visible?'Hide password':'Show password'} onClick={()=>setVisible(!visible)}>{visible?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label>{error&&<p className="error" role="alert">{error}</p>}<button className="primary full" disabled={busy}>{busy?'Signing you in…':'Open my workspace'}<ArrowRight size={18}/></button></form><div className="login-footer"><ShieldCheck size={15}/>A secure workspace for your restaurant team</div></div></div>;
}
